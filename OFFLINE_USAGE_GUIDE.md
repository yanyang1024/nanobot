# nanobot 离线使用指南

> 本文档针对内网/离线环境下的 nanobot 使用，基于实际测试验证

## 目录

1. [快速开始](#快速开始)
2. [核心工具使用](#核心工具使用)
3. [离线可用技能](#离线可用技能)
4. [实战示例](#实战示例)
5. [常见问题](#常见问题)

---

## 快速开始

### 基本对话

```bash
nanobot agent -m "你好，请介绍一下你自己"
```

### 交互模式

```bash
nanobot agent
# 进入交互式对话界面
```

### 清除会话

在对话中输入 `/new` 开始新会话

---

## 核心工具使用

### 1. 文件系统工具

#### 1.1 列出目录 (list_dir)

```bash
nanobot agent -m "请列出当前工作区的所有文件和目录"
```

**适用场景**：
- 查看项目结构
- 找到特定文件
- 浏览目录内容

#### 1.2 读取文件 (read_file)

```bash
nanobot agent -m "请读取 SOUL.md 文件的内容"
nanobot agent -m "使用 read_file 工具查看 README.md 的前 50 行"
```

**适用场景**：
- 查看配置文件
- 阅读文档
- 检查日志文件

#### 1.3 写入文件 (write_file)

```bash
nanobot agent -m "请创建一个新文件 notes.md，内容是 '# 今日笔记\\n\\n今天完成了 nanobot 部署'"
```

**适用场景**：
- 生成报告
- 创建配置
- 保存笔记

#### 1.4 编辑文件 (edit_file)

```bash
nanobot agent -m "请修改 notes.md 文件，将'今日笔记'替换为'工作记录'"
```

**注意事项**：
- old_text 必须精确匹配（包括空格和换行）
- 如果有多个匹配，会要求更精确的上下文
- 建议包含足够的上下文使替换唯一

### 2. 命令行工具 (exec)

#### 2.1 基本命令执行

```bash
nanobot agent -m "请执行命令：ls -la"
nanobot agent -m "使用 exec 工具查看当前时间：date"
```

#### 2.2 系统监控

```bash
nanobot agent -m "请查看磁盘使用情况：df -h"
nanobot agent -m "执行命令查看内存使用：free -h"
nanobot agent -m "查看系统负载：uptime"
```

#### 2.3 文件操作

```bash
nanobot agent -m "统计当前目录的文件数量：find . -type f | wc -l"
nanobot agent -m "查找最大的5个文件：du -ah . | sort -rh | head -5"
```

**注意事项**：
- 默认超时时间：60 秒
- 工作目录：`~/.nanobot/workspace`
- 需要权限的操作可能失败

### 3. Markdown API 工具 (md_read / md_write)

> 专用于远程知识库操作的定制工具

#### 3.1 读取远程文档

```bash
nanobot agent -m "请使用 md_read 工具读取 biz/knowledge.md 文件"
```

#### 3.2 写入远程文档

```bash
nanobot agent -m "请使用 md_write 工具在 reports/ 目录下创建 summary.md，内容是..."
```

**配置要求**：
- md-api 服务地址：`http://0.0.0.0:18081`
- Token：`replace-with-strong-token`（已硬编码）
- 知识库根目录：`/home/yy/ss/nanobot/bot_api/data/knowledgeA`

### 4. Memory 工具

#### 4.1 保存记忆

```bash
nanobot agent -m "请保存一条记忆：项目已成功部署，使用 Ollama qwen3:14b 模型"
```

实际执行：
```bash
nanobot agent -m "使用 write_file 工具在 memory/MEMORY.md 追加：\\n\\n## 2026-03-02\\n项目已成功部署"
```

#### 4.2 读取记忆

```bash
nanobot agent -m "请使用 read_file 工具读取 memory/MEMORY.md，告诉我最近记录了什么"
```

**记忆结构**：
- `MEMORY.md` - 长期事实（总是加载到上下文）
- `HISTORY.md` - 事件日志（需用 grep 搜索）

### 5. Spawn 工具（子代理）

```bash
nanobot agent -m "请使用 spawn 工具创建一个子代理，任务是：分析所有 .md 文件并生成索引"
```

**适用场景**：
- 耗时较长的任务
- 独立运行的子任务
- 并行处理多个任务

---

## 离线可用技能

### Memory 技能

**特点**：
- 标记为 `always: true`，总是激活
- 双层记忆结构
- 支持长期事实和事件日志

**使用方式**：
```bash
# 查看技能说明
nanobot agent -m "请读取 skills/memory/SKILL.md 文件"

# 使用技能功能
nanobot agent -m "使用 write_file 更新 MEMORY.md，保存重要信息"
```

### Daily-md-writer 技能

**用途**：读取业务知识库并生成日报/周报

**示例**：
```bash
nanobot agent -m "请读取 biz/knowledge.md 和 biz/milestones.md，生成本周周报并写入 reports/weekly.md"
```

### Skill-creator 技能

**用途**：创建新的自定义技能

**示例**：
```bash
nanobot agent -m "请使用 skill-creator 创建一个新技能，名称为 log-analyzer，描述是'分析日志文件'"
```

### 不适合离线的技能

以下技能因依赖外部服务，离线环境无法使用：

| 技能 | 原因 | 替代方案 |
|------|------|----------|
| weather | 需要访问 wttr.in | 使用本机气象数据 |
| summarize | 需要外部 summarize.sh 工具 | 使用 LLM 直接总结 |
| github | 需要 gh CLI 和网络 | 使用本地 git 命令 |
| tmux | 需要 tmux 会话 | 使用 screen 或直接执行 |

---

## 实战示例

### 示例 1：生成项目周报

```bash
nanobot agent -m "请执行以下任务：
1. 使用 md_read 读取 biz/knowledge.md
2. 使用 md_read 读取 biz/milestones.md
3. 基于这两份文件生成周报
4. 使用 md_write 将周报写入 reports/weekly-$(date +%Y-W%V).md"
```

### 示例 2：批量文件处理

```bash
nanobot agent -m "请执行以下任务：
1. 使用 exec 工具列出所有 .txt 文件：find . -name '*.txt'
2. 使用 read_file 读取每个文件
3. 统计总行数和字数
4. 使用 write_file 生成报告到 reports/file-stats.md"
```

### 示例 3：系统监控报告

```bash
nanobot agent -m "请生成系统监控报告：
1. 使用 exec 执行 df -h 查看磁盘
2. 使用 exec 执行 free -h 查看内存
3. 使用 exec 执行 uptime 查看负载
4. 汇总信息并使用 write_file 保存到 reports/system-status.md"
```

### 示例 4：日志分析

```bash
nanobot agent -m "请分析日志文件：
1. 使用 exec 查找最近的错误：grep -i error ~/.nanobot/logs/tool_trace.jsonl | tail -10
2. 使用 read_file 读取相关日志
3. 总结问题并使用 write_file 保存到 reports/error-summary.md"
```

### 示例 5：备份重要文件

```bash
nanobot agent -m "请执行备份任务：
1. 使用 list_dir 列出 memory/ 目录
2. 使用 exec 创建备份：tar -czf backup-$(date +%Y%m%d).tar.gz memory/
3. 使用 exec 验证备份：tar -tzf backup-*.tar.gz | head -20"
```

---

## 常见问题

### Q1: 工具调用后没有返回内容

**现象**：
```
🐈 nanobot
I've completed processing but have no response to give.
```

**原因**：模型调用工具后没有生成最终响应

**解决方法**：
1. 明确要求模型返回结果："请执行任务并返回结果"
2. 分步骤执行，每步都要求确认
3. 使用更详细的提示词

### Q2: 文件编辑失败

**错误信息**：`Error: old_text not found`

**解决方法**：
1. 确保精确匹配（包括空格、换行）
2. 包含更多上下文使其唯一
3. 先用 read_file 查看确切内容

### Q3: 命令执行超时

**默认超时**：60 秒

**解决方法**：
```bash
# 配置文件中修改
~/.nanobot/config.json
{
  "tools": {
    "exec": {
      "timeout": 120  # 增加到 120 秒
    }
  }
}
```

### Q4: 无法访问工作区外的文件

**默认限制**：`restrictToWorkspace: true`

**解决方法**：
```bash
# 修改配置允许访问
~/.nanobot/config.json
{
  "tools": {
    "restrictToWorkspace": false
  }
}
```

⚠️ **安全警告**：关闭限制前请确保理解安全风险

### Q5: 工具调用历史查看

```bash
# 查看最近 100 条工具调用
nanobot trace -n 100

# 或直接查看日志文件
cat ~/.nanobot/logs/tool_trace.jsonl | tail -20
```

### Q6: 会话管理

```bash
# 清除当前会话
echo "/new"

# 查看会话文件
ls ~/.nanobot/workspace/sessions/

# 查看特定会话
cat ~/.nanobot/workspace/sessions/cli_direct.jsonl
```

---

## 最佳实践

### 1. 明确的工具调用指令

❌ **不好的示例**：
```bash
nanobot agent -m "看看工作区有什么"
```

✅ **好的示例**：
```bash
nanobot agent -m "请使用 list_dir 工具列出当前工作区的所有文件和目录"
```

### 2. 分步骤执行复杂任务

❌ **不好的示例**：
```bash
nanobot agent -m "分析所有日志并生成报告"
```

✅ **好的示例**：
```bash
nanobot agent -m "请按以下步骤执行：
1. 使用 exec 查找所有日志文件
2. 使用 read_file 读取最近的错误日志
3. 分析错误模式
4. 使用 write_file 生成分析报告"
```

### 3. 利用记忆功能

```bash
# 保存重要配置
nanobot agent -m "在 MEMORY.md 中记录：Ollama 服务地址是 http://172.24.16.1:11434"

# 后续查询
nanobot agent -m "查看 MEMORY.md，告诉我 Ollama 的地址"
```

### 4. 定期备份

```bash
# 每周备份记忆和会话
nanobot agent -m "请执行备份：tar -czf backup-$(date +%Y%m%d).tar.gz memory/ sessions/"
```

### 5. 工具组合使用

```bash
# 文件 -> 处理 -> 新文件
nanobot agent -m "请：
1. read_file 读取 data.txt
2. 分析数据
3. write_file 生成 processed_data.md"
```

---

## 配置参考

### 完整配置示例

```json
{
  "agents": {
    "defaults": {
      "model": "qwen3:14b",
      "maxTokens": 8192,
      "temperature": 0.1,
      "maxToolIterations": 40,
      "memoryWindow": 100
    }
  },
  "providers": {
    "ollama": {
      "apiBase": "http://172.24.16.1:11434",
      "apiKey": "ollama"
    }
  },
  "tools": {
    "exec": {
      "timeout": 60
    },
    "restrictToWorkspace": true,
    "web": {
      "search": {
        "maxResults": 5
      }
    }
  }
}
```

### 性能调优

| 参数 | 默认值 | 推荐值（离线） | 说明 |
|------|--------|---------------|------|
| temperature | 0.1 | 0.0-0.2 | 越低越确定 |
| maxTokens | 8192 | 4096-8192 | 根据需求调整 |
| maxToolIterations | 40 | 20-40 | 工具调用上限 |
| memoryWindow | 100 | 50-100 | 历史消息窗口 |

---

## 附录

### A. 工具速查表

| 工具名 | 功能 | 示例 |
|--------|------|------|
| read_file | 读取文件 | 读取配置文件 |
| write_file | 写入文件 | 创建新文档 |
| edit_file | 编辑文件 | 替换内容 |
| list_dir | 列出目录 | 查看文件列表 |
| exec | 执行命令 | 运行 shell 命令 |
| md_read | 读取远程 MD | 读取知识库 |
| md_write | 写入远程 MD | 保存报告 |
| spawn | 创建子代理 | 后台任务 |

### B. 技能速查表

| 技能 | 状态 | 依赖 |
|------|------|------|
| memory | ✅ 可用 | 无 |
| daily-md-writer | ✅ 可用 | md-api |
| skill-creator | ✅ 可用 | 无 |
| cron | ⚠️ 部分 | gateway |
| weather | ❌ 离线不可用 | wttr.in |
| summarize | ❌ 离线不可用 | 外部 CLI |
| github | ❌ 离线不可用 | gh CLI |
| tmux | ❌ 离线不可用 | tmux |

### C. 故障排查命令

```bash
# 测试 Ollama 连接
curl http://172.24.16.1:11434/api/tags

# 测试 md-api 连接
curl -X POST http://0.0.0.0:18081/read \
  -H "X-API-Token: replace-with-strong-token" \
  -H "Content-Type: application/json" \
  -d '{"path": "biz/knowledge.md"}'

# 查看工具调用日志
tail -f ~/.nanobot/logs/tool_trace.jsonl

# 检查配置文件
cat ~/.nanobot/config.json | python3 -m json.tool

# 测试基本对话
nanobot agent -m "你好"
```

---

**文档版本**: 1.0
**最后更新**: 2026-03-02
**适用版本**: nanobot 0.1.4.post1
